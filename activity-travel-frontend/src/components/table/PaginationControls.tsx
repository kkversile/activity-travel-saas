import type { PaginationMeta } from "@/types/api";

export function PaginationControls({ meta, onPageChange, onPageSizeChange }: { meta: PaginationMeta; onPageChange: (page: number) => void; onPageSizeChange: (pageSize: number) => void }) { return <div className="pagination"><span>{meta.totalItems} records · Page {meta.page} of {meta.totalPages}</span><label>Rows<select value={meta.pageSize} onChange={(event) => onPageSizeChange(Number(event.target.value))}><option value="10">10</option><option value="25">25</option><option value="50">50</option><option value="100">100</option></select></label><button type="button" disabled={!meta.hasPreviousPage} onClick={() => onPageChange(meta.page - 1)}>Previous</button><button type="button" disabled={!meta.hasNextPage} onClick={() => onPageChange(meta.page + 1)}>Next</button></div>; }
