"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { createCatalog, updateCatalog } from "@/services/catalogService";
import { catalogRoute } from "@/services/catalogService";
import type { CatalogKind, CatalogRecord } from "@/services/catalogService";
export function CatalogForm({ kind, mode, id, initial }: { kind: CatalogKind; mode: "create" | "edit"; id?: string; initial?: CatalogRecord }) { const router = useRouter(); const destination = kind === "destinations"; const route = catalogRoute(kind); const [form, setForm] = useState({ name: initial?.name ?? "", slug: initial?.slug ?? "", description: initial?.description ?? "", status: initial?.status ?? "ACTIVE", displayOrder: String(initial?.displayOrder ?? 0), country: initial?.country ?? "", state: initial?.state ?? "", city: initial?.city ?? "", timezone: initial?.timezone ?? "Asia/Kolkata", latitude: initial?.latitude === undefined ? "" : String(initial.latitude), longitude: initial?.longitude === undefined ? "" : String(initial.longitude) }); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); const update = (field: keyof typeof form, value: string) => setForm((current) => ({ ...current, [field]: value })); async function submit(event: FormEvent) { event.preventDefault(); setError(""); setSaving(true); try { const payload = destination ? { country: form.country, state: form.state || undefined, city: form.city, timezone: form.timezone, latitude: form.latitude || undefined, longitude: form.longitude || undefined, description: form.description || undefined, status: form.status } : { name: form.name, slug: form.slug, description: form.description || undefined, displayOrder: Number(form.displayOrder), status: form.status }; if (mode === "create") await createCatalog(kind, payload); else if (id) await updateCatalog(kind, id, payload); router.push(`/${route}`); } catch (reason) { setError(reason instanceof Error ? reason.message : "Unable to save record"); } finally { setSaving(false); } } return <div><div className="page-heading"><div><p className="eyebrow">CATALOGUE</p><h2>{mode === "create" ? "Create" : "Edit"} {destination ? "destination" : "category"}</h2></div></div><form className="panel form-grid" onSubmit={submit}>{destination ? <><label>Country<input value={form.country} onChange={(event) => update("country", event.target.value)} required /></label><label>State / region<input value={form.state} onChange={(event) => update("state", event.target.value)} /></label><label>City<input value={form.city} onChange={(event) => update("city", event.target.value)} required /></label><label>Timezone<input value={form.timezone} onChange={(event) => update("timezone", event.target.value)} required /></label><label>Latitude<input type="number" step="any" value={form.latitude} onChange={(event) => update("latitude", event.target.value)} /></label><label>Longitude<input type="number" step="any" value={form.longitude} onChange={(event) => update("longitude", event.target.value)} /></label></> : <><label>Name<input value={form.name} onChange={(event) => update("name", event.target.value)} required minLength={2} /></label><label>Slug<input value={form.slug} onChange={(event) => update("slug", event.target.value)} required minLength={2} /></label><label>Display order<input type="number" min="0" value={form.displayOrder} onChange={(event) => update("displayOrder", event.target.value)} /></label></>}<label>Status<select value={form.status} onChange={(event) => update("status", event.target.value)}><option value="ACTIVE">Active</option><option value="INACTIVE">Inactive</option><option value="ARCHIVED">Archived</option></select></label><label className="full-span">Description<textarea value={form.description} onChange={(event) => update("description", event.target.value)} /></label>{error && <div className="notice error">{error}</div>}<div className="form-actions"><button type="button" onClick={() => router.back()}>Cancel</button><button className="primary" disabled={saving}>{saving ? "Saving…" : "Save"}</button></div></form></div>; }
