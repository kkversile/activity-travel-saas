import { useEffect, useState } from 'react';
import { API_BASE, api, type AgentBooking } from '../api';
import { Badge, ErrorBox, Loading, Panel, shortDate, money } from '../components/Ui';

const cancellable = ['CONFIRMED', 'PENDING_VENDOR_CONFIRMATION', 'PENDING_MANUAL_REVIEW'];
const categories = ['CUSTOMER_REQUEST', 'DUPLICATE', 'OTHER'];

export default function AgentBookings() {
  const [rows, setRows] = useState<AgentBooking[]>([]); const [selected, setSelected] = useState<AgentBooking | null>(null); const [voucher, setVoucher] = useState<any>(null); const [preview, setPreview] = useState<any>(null); const [cancellationKey, setCancellationKey] = useState<string | null>(null); const [category, setCategory] = useState(categories[0]); const [reason, setReason] = useState(''); const [acknowledged, setAcknowledged] = useState(false); const [recipient, setRecipient] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(true); const [busy, setBusy] = useState(false);
  async function load() { setLoading(true); try { setRows(await api.request<AgentBooking[]>('/agent/bookings')); } catch (e) { setError(e instanceof Error ? e.message : String(e)); } finally { setLoading(false); } }
  useEffect(() => { void load(); }, []);
  function closeModal() { setSelected(null); setVoucher(null); setPreview(null); setCancellationKey(null); }
  async function open(booking: AgentBooking) { setSelected(booking); setError(''); try { setVoucher(await api.request(`/agent/bookings/${booking.id}/voucher`)); } catch (e) { setError((e as Error).message); } }
  async function openCancellation(booking: AgentBooking) { await open(booking); setPreview(null); setCancellationKey(crypto.randomUUID()); setCategory(categories[0]); setReason(''); setAcknowledged(false); try { setPreview(await api.request(`/agent/bookings/${booking.id}/cancellation/preview`, { method: 'POST' })); } catch (e) { setError(e instanceof Error ? e.message : String(e)); } }
  async function cancelBooking() { if (!selected || !preview?.eligible || !cancellationKey || !acknowledged || !reason.trim()) return; setBusy(true); try { const result = await api.request<AgentBooking>(`/agent/bookings/${selected.id}/cancellation`, { method: 'POST', headers: { 'Idempotency-Key': cancellationKey }, body: JSON.stringify({ expectedCancellationFingerprint: preview.cancellationFingerprint, reasonCategory: category, reason, acknowledged }) }); setSelected(result); setVoucher(await api.request(`/agent/bookings/${result.id}/voucher`)); setPreview(null); setCancellationKey(null); await load(); } catch (e) { setError(e instanceof Error ? e.message : String(e)); } finally { setBusy(false); } }
  async function share(channel: 'DOWNLOAD' | 'EMAIL' | 'WHATSAPP') { if (!selected || !voucher?.currentVoucher) return; setBusy(true); try { const result = await api.request<any>(`/agent/bookings/${selected.id}/voucher/share`, { method: 'POST', body: JSON.stringify({ channel, ...(channel !== 'DOWNLOAD' ? { recipient } : {}) }) }); if (channel === 'DOWNLOAD') { const blob = await api.blob(`/files/${voucher.currentVoucher.fileId}/content`); const url = URL.createObjectURL(blob); const link = document.createElement('a'); link.href = url; link.download = `${selected.bookingCode}-voucher-v${voucher.currentVoucher.version}.pdf`; link.click(); URL.revokeObjectURL(url); } else setError(result.status === 'QUEUED' ? 'Queued for delivery' : ''); } catch (e) { setError((e as Error).message); } finally { setBusy(false); } }
  if (loading) return <Loading />;
  return <>{error && <ErrorBox error={error} />}<Panel title="My bookings"><div className="table-wrap"><table><thead><tr><th>Booking</th><th>Product</th><th>Service date</th><th>Amount</th><th>Mode</th><th>Status</th><th /></tr></thead><tbody>{rows.map((row) => <tr key={row.id}><td className="mono">{row.bookingCode}</td><td>{row.snapshot?.productSnapshot?.productName || '—'}</td><td>{shortDate(row.serviceDate)}</td><td>{money(row.amount || 0)}</td><td>{row.bookingMode?.replaceAll('_', ' ') || 'Legacy'}</td><td><Badge value={row.status} /></td><td><button className="btn small" onClick={() => void open(row)}>Details</button>{cancellable.includes(row.status) && !row.cancellation && <button className="btn small" onClick={() => void openCancellation(row)}>Cancel</button>}</td></tr>)}</tbody></table></div>{!rows.length && <div className="empty">No bookings created by this agent yet.</div>}</Panel>{selected && <div className="modal-backdrop" onClick={closeModal}><div className="voucher-modal booking-detail" onClick={(e) => e.stopPropagation()}><div className="modal-head"><h3>{selected.bookingCode}</h3><button className="btn small" onClick={closeModal}>Close</button></div><p><Badge value={selected.status} /> · {selected.bookingMode?.replaceAll('_', ' ') || 'Legacy'} · {shortDate(selected.serviceDate)}</p><h4>{selected.snapshot?.productSnapshot?.productName} · {selected.snapshot?.variantSnapshot?.name}</h4><p><b>Price:</b> {money(selected.amount || 0)} {selected.currency}</p><h4>Voucher</h4>{voucher?.reason && <p className="muted">{voucher.reason.replaceAll('_', ' ')}</p>}{voucher?.currentVoucher ? <><p><Badge value="CURRENT" /> Version {voucher.currentVoucher.version} · {new Date(voucher.currentVoucher.generatedAt).toLocaleString()}</p><p className="mono">{voucher.currentVoucher.voucherCode}</p><div className="actions"><button className="btn primary" disabled={busy} onClick={() => void share('DOWNLOAD')}>Download Voucher</button><input placeholder="recipient email or +E.164 phone" value={recipient} onChange={(e) => setRecipient(e.target.value)} /><button className="btn" disabled={busy || !recipient} onClick={() => void share('EMAIL')}>Email</button><button className="btn" disabled={busy || !recipient} onClick={() => void share('WHATSAPP')}>WhatsApp</button></div></> : <p className="muted">{voucher?.readiness?.ready ? 'Generating' : 'Waiting for supplier evidence'}</p>}{voucher?.travellerEvidence?.length ? <><h4>Traveller ticket / evidence</h4>{voucher.travellerEvidence.map((item: any) => <div key={`${item.kind}-${item.versionNumber}`}>{item.kind} v{item.versionNumber} {item.referenceValue || ''} {item.fileId && <button className="link-button" onClick={() => void api.blob(`/files/${item.fileId}/content`).then((blob) => { const url = URL.createObjectURL(blob); window.open(url, '_blank'); })}>Download file</button>}</div>)}</> : null}{selected.cancellation ? <><h4>Cancellation</h4><p>{selected.cancellation.initiator} · {selected.cancellation.reasonCategory}</p></> : preview && <section><h4>Cancellation preview</h4><p>Expected refund: {money(preview.refundEntitlement || 0)} {preview.currency}</p><label className="field"><span>Reason category</span><select value={category} onChange={(e) => setCategory(e.target.value)}>{categories.map((item) => <option key={item}>{item}</option>)}</select></label><label className="field"><span>Reason</span><textarea value={reason} onChange={(e) => setReason(e.target.value)} required /></label><label className="check-row"><input type="checkbox" checked={acknowledged} onChange={(e) => setAcknowledged(e.target.checked)} /> I understand the cancellation outcome.</label><button className="btn accent" disabled={busy || !acknowledged || !reason.trim()} onClick={() => void cancelBooking()}>Confirm cancellation</button></section>}<h4>Travellers</h4>{selected.travellers.map((traveller, index) => <div key={index}>{traveller.fullName || `Traveller ${index + 1}`} · {traveller.travellerType}</div>)}<h4>Timeline</h4>{selected.timeline.map((event, index) => <div key={index} className="timeline-row"><b>{event.eventType}</b><span>{new Date(event.createdAt).toLocaleString()}</span></div>)}</div></div>}</>;
}
