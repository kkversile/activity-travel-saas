import { Body, Controller, Delete, Get, Param, Patch, Post, Query, UseGuards } from "@nestjs/common"; import { ApiHeader, ApiTags } from "@nestjs/swagger"; import { UserRole } from "@prisma/client"; import { AccessTokenGuard } from "../../auth/auth.guard"; import { Roles } from "../../auth/roles.decorator"; import { RolesGuard } from "../../auth/roles.guard"; import { TenantAccessGuard } from "../../auth/tenant.guard"; import { TenantContext, TenantContextValue } from "../../common/tenant-context"; import { AgentCommissionsService } from "./agent-commissions.service"; import { CommissionQueryDto, CreateCommissionDto, UpdateCommissionDto } from "./dto/commission.dto";
@ApiTags("agent-commissions") @ApiHeader({ name: "x-tenant-id", required: true }) @Controller("agent-commissions") @UseGuards(AccessTokenGuard, TenantAccessGuard, RolesGuard)
export class AgentCommissionsController { constructor(private readonly commissions: AgentCommissionsService) {} @Get() list(@TenantContext() c: TenantContextValue, @Query() q: CommissionQueryDto) { return this.commissions.list(c.tenantId, q); } @Post() @Roles(UserRole.PARTNER_ADMIN) create(@TenantContext() c: TenantContextValue, @Body() d: CreateCommissionDto) { return this.commissions.create(c.tenantId, d); } @Get(":id") get(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.commissions.get(c.tenantId, id); } @Patch(":id") @Roles(UserRole.PARTNER_ADMIN) update(@TenantContext() c: TenantContextValue, @Param("id") id: string, @Body() d: UpdateCommissionDto) { return this.commissions.update(c.tenantId, id, d); } @Delete(":id") @Roles(UserRole.PARTNER_ADMIN) remove(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.commissions.remove(c.tenantId, id); } }
