import { Body, Controller, Delete, Get, Param, Patch, Post, Query, UseGuards } from "@nestjs/common"; import { ApiHeader, ApiTags } from "@nestjs/swagger"; import { UserRole } from "@prisma/client"; import { AccessTokenGuard } from "../../auth/auth.guard"; import { Roles } from "../../auth/roles.decorator"; import { RolesGuard } from "../../auth/roles.guard"; import { TenantAccessGuard } from "../../auth/tenant.guard"; import { TenantContext, TenantContextValue } from "../../common/tenant-context"; import { AgentQueryDto, CreateAgentDto, UpdateAgentDto } from "./dto/agent.dto"; import { AgentsService } from "./agents.service";
@ApiTags("agents") @ApiHeader({ name: "x-tenant-id", required: true }) @Controller("agents") @UseGuards(AccessTokenGuard, TenantAccessGuard, RolesGuard)
export class AgentsController { constructor(private readonly agents: AgentsService) {} @Get() list(@TenantContext() c: TenantContextValue, @Query() q: AgentQueryDto) { return this.agents.list(c.tenantId, q); } @Post() @Roles(UserRole.PARTNER_ADMIN) create(@TenantContext() c: TenantContextValue, @Body() d: CreateAgentDto) { return this.agents.create(c.tenantId, d); } @Get(":id") get(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.agents.get(c.tenantId, id); } @Patch(":id") @Roles(UserRole.PARTNER_ADMIN) update(@TenantContext() c: TenantContextValue, @Param("id") id: string, @Body() d: UpdateAgentDto) { return this.agents.update(c.tenantId, id, d); } @Delete(":id") @Roles(UserRole.PARTNER_ADMIN) remove(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.agents.remove(c.tenantId, id); } }
