import { Body, Controller, Delete, Get, Param, Patch, Post, Query, UseGuards } from "@nestjs/common";
import { ApiHeader, ApiTags } from "@nestjs/swagger";
import { UserRole } from "@prisma/client";
import { AccessTokenGuard } from "../../auth/auth.guard"; import { Roles } from "../../auth/roles.decorator"; import { RolesGuard } from "../../auth/roles.guard"; import { TenantAccessGuard } from "../../auth/tenant.guard"; import { TenantContext, TenantContextValue } from "../../common/tenant-context"; import { CreateVoucherModuleDto, UpdateVoucherDto, VoucherQueryDto } from "./dto/voucher.dto"; import { VouchersService } from "./vouchers.service";
@ApiTags("vouchers") @ApiHeader({ name: "x-tenant-id", required: true }) @Controller("vouchers") @UseGuards(AccessTokenGuard, TenantAccessGuard, RolesGuard)
export class VouchersController { constructor(private readonly vouchers: VouchersService) {} @Get() list(@TenantContext() c: TenantContextValue, @Query() q: VoucherQueryDto) { return this.vouchers.list(c.tenantId, q); } @Post() @Roles(UserRole.PARTNER_ADMIN, UserRole.ACTIVITY_MANAGER) create(@TenantContext() c: TenantContextValue, @Body() d: CreateVoucherModuleDto) { return this.vouchers.create(c.tenantId, d); } @Get(":id") get(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.vouchers.get(c.tenantId, id); } @Patch(":id") @Roles(UserRole.PARTNER_ADMIN, UserRole.ACTIVITY_MANAGER) update(@TenantContext() c: TenantContextValue, @Param("id") id: string, @Body() d: UpdateVoucherDto) { return this.vouchers.update(c.tenantId, id, d); } @Delete(":id") @Roles(UserRole.PARTNER_ADMIN, UserRole.ACTIVITY_MANAGER) remove(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.vouchers.remove(c.tenantId, id); } }
