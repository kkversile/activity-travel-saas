import { Body, Controller, Delete, Get, Param, Post, Query, UseGuards } from "@nestjs/common"; import { ApiHeader, ApiTags } from "@nestjs/swagger"; import { UserRole } from "@prisma/client"; import { AccessTokenGuard } from "../../auth/auth.guard"; import { Roles } from "../../auth/roles.decorator"; import { RolesGuard } from "../../auth/roles.guard"; import { TenantAccessGuard } from "../../auth/tenant.guard"; import { TenantContext, TenantContextValue } from "../../common/tenant-context"; import { BlackoutDatesService } from "./blackout-dates.service"; import { BlackoutQueryDto } from "./dto/blackout-query.dto"; import { CreateBlackoutDateDto } from "./dto/blackout.dto";
@ApiTags("blackout-dates") @ApiHeader({ name: "x-tenant-id", required: true }) @Controller("blackout-dates") @UseGuards(AccessTokenGuard, TenantAccessGuard, RolesGuard)
export class BlackoutDatesController { constructor(private readonly dates: BlackoutDatesService) {} @Get() list(@TenantContext() c: TenantContextValue, @Query() q: BlackoutQueryDto) { return this.dates.list(c.tenantId, q); } @Post() @Roles(UserRole.PARTNER_ADMIN, UserRole.ACTIVITY_MANAGER) create(@TenantContext() c: TenantContextValue, @Body() d: CreateBlackoutDateDto) { return this.dates.create(c.tenantId, d); } @Get(":id") get(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.dates.get(c.tenantId, id); } @Delete(":id") @Roles(UserRole.PARTNER_ADMIN, UserRole.ACTIVITY_MANAGER) remove(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.dates.remove(c.tenantId, id); } }
