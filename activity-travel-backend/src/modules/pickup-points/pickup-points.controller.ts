import { Body, Controller, Delete, Get, Param, Patch, Post, Query, UseGuards } from "@nestjs/common"; import { ApiHeader, ApiTags } from "@nestjs/swagger"; import { UserRole } from "@prisma/client"; import { AccessTokenGuard } from "../../auth/auth.guard"; import { Roles } from "../../auth/roles.decorator"; import { RolesGuard } from "../../auth/roles.guard"; import { TenantAccessGuard } from "../../auth/tenant.guard"; import { TenantContext, TenantContextValue } from "../../common/tenant-context"; import { CreatePickupDto, PickupQueryDto, UpdatePickupDto } from "./dto/pickup.dto"; import { PickupPointsService } from "./pickup-points.service";
@ApiTags("pickup-points") @ApiHeader({ name: "x-tenant-id", required: true }) @Controller("pickup-points") @UseGuards(AccessTokenGuard, TenantAccessGuard, RolesGuard)
export class PickupPointsController { constructor(private readonly points: PickupPointsService) {} @Get() list(@TenantContext() c: TenantContextValue, @Query() q: PickupQueryDto) { return this.points.list(c.tenantId, q); } @Post() @Roles(UserRole.PARTNER_ADMIN, UserRole.ACTIVITY_MANAGER) create(@TenantContext() c: TenantContextValue, @Body() d: CreatePickupDto) { return this.points.create(c.tenantId, d); } @Get(":id") get(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.points.get(c.tenantId, id); } @Patch(":id") @Roles(UserRole.PARTNER_ADMIN, UserRole.ACTIVITY_MANAGER) update(@TenantContext() c: TenantContextValue, @Param("id") id: string, @Body() d: UpdatePickupDto) { return this.points.update(c.tenantId, id, d); } @Delete(":id") @Roles(UserRole.PARTNER_ADMIN) remove(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.points.remove(c.tenantId, id); } }
