import { Controller, Get, Query, UseGuards } from "@nestjs/common"; import { ApiHeader, ApiTags } from "@nestjs/swagger"; import { AccessTokenGuard } from "../../auth/auth.guard"; import { RolesGuard } from "../../auth/roles.guard"; import { TenantAccessGuard } from "../../auth/tenant.guard"; import { TenantContext, TenantContextValue } from "../../common/tenant-context"; import { AuditLogsService } from "./audit-logs.service"; import { AuditQueryDto } from "./dto/audit-query.dto";
@ApiTags("audit-logs") @ApiHeader({ name: "x-tenant-id", required: true }) @Controller("audit-logs") @UseGuards(AccessTokenGuard, TenantAccessGuard, RolesGuard)
export class AuditLogsController { constructor(private readonly logs: AuditLogsService) {} @Get() list(@TenantContext() c: TenantContextValue, @Query() q: AuditQueryDto) { return this.logs.list(c.tenantId, q); } }
