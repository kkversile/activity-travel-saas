import { Injectable } from "@nestjs/common"; import { PrismaService } from "../../prisma/prisma.service"; import { paginated, parsePaginationQuery } from "../../common/pagination/pagination";
@Injectable() export class AuditLogsService { constructor(private readonly prisma: PrismaService) {} async list(tenantId: string, query: { page?: number; pageSize?: number; search?: string; entityType?: string; entityId?: string; sortBy?: string; sortOrder?: "asc" | "desc" }) { const p = parsePaginationQuery(query, ["createdAt", "action", "entityType"], "createdAt"); const where = { tenantId, ...(query.entityType ? { entityType: query.entityType } : {}), ...(query.entityId ? { entityId: query.entityId } : {}), ...(p.search ? { OR: [{ action: { contains: p.search, mode: "insensitive" as const } }, { entityType: { contains: p.search, mode: "insensitive" as const } }] } : {}) }; const [data, total] = await this.prisma.$transaction([this.prisma.auditLog.findMany({ where, orderBy: { [p.sortBy]: p.sortOrder }, skip: (p.page - 1) * p.pageSize, take: p.pageSize }), this.prisma.auditLog.count({ where })]); return paginated(data, p.page, p.pageSize, total); } }
