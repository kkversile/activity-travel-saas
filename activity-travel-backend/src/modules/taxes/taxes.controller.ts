import { Body, Controller, Delete, Get, Param, Patch, Post, Query, UseGuards } from "@nestjs/common"; import { ApiHeader, ApiTags } from "@nestjs/swagger"; import { UserRole } from "@prisma/client"; import { AccessTokenGuard } from "../../auth/auth.guard"; import { Roles } from "../../auth/roles.decorator"; import { RolesGuard } from "../../auth/roles.guard"; import { TenantAccessGuard } from "../../auth/tenant.guard"; import { TenantContext, TenantContextValue } from "../../common/tenant-context"; import { TaxesService } from "./taxes.service"; import { CreateTaxDto, TaxQueryDto, UpdateTaxDto } from "./dto/tax.dto";
@ApiTags("taxes") @ApiHeader({ name: "x-tenant-id", required: true }) @Controller("taxes") @UseGuards(AccessTokenGuard, TenantAccessGuard, RolesGuard)
export class TaxesController { constructor(private readonly taxes: TaxesService) {} @Get() list(@TenantContext() c: TenantContextValue, @Query() q: TaxQueryDto) { return this.taxes.list(c.tenantId, q); } @Post() @Roles(UserRole.PARTNER_ADMIN) create(@TenantContext() c: TenantContextValue, @Body() d: CreateTaxDto) { return this.taxes.create(c.tenantId, d); } @Get(":id") get(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.taxes.get(c.tenantId, id); } @Patch(":id") @Roles(UserRole.PARTNER_ADMIN) update(@TenantContext() c: TenantContextValue, @Param("id") id: string, @Body() d: UpdateTaxDto) { return this.taxes.update(c.tenantId, id, d); } @Delete(":id") @Roles(UserRole.PARTNER_ADMIN) remove(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.taxes.remove(c.tenantId, id); } }
