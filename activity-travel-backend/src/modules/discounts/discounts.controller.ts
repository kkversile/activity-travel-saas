import { Body, Controller, Delete, Get, Param, Patch, Post, Query, UseGuards } from "@nestjs/common"; import { ApiHeader, ApiTags } from "@nestjs/swagger"; import { UserRole } from "@prisma/client"; import { AccessTokenGuard } from "../../auth/auth.guard"; import { Roles } from "../../auth/roles.decorator"; import { RolesGuard } from "../../auth/roles.guard"; import { TenantAccessGuard } from "../../auth/tenant.guard"; import { TenantContext, TenantContextValue } from "../../common/tenant-context"; import { DiscountsService } from "./discounts.service"; import { CreateDiscountDto, DiscountQueryDto, UpdateDiscountDto } from "./dto/discount.dto";
@ApiTags("discounts") @ApiHeader({ name: "x-tenant-id", required: true }) @Controller("discounts") @UseGuards(AccessTokenGuard, TenantAccessGuard, RolesGuard)
export class DiscountsController { constructor(private readonly discounts: DiscountsService) {} @Get() list(@TenantContext() c: TenantContextValue, @Query() q: DiscountQueryDto) { return this.discounts.list(c.tenantId, q); } @Post() @Roles(UserRole.PARTNER_ADMIN, UserRole.ACTIVITY_MANAGER) create(@TenantContext() c: TenantContextValue, @Body() d: CreateDiscountDto) { return this.discounts.create(c.tenantId, d); } @Get(":id") get(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.discounts.get(c.tenantId, id); } @Patch(":id") @Roles(UserRole.PARTNER_ADMIN, UserRole.ACTIVITY_MANAGER) update(@TenantContext() c: TenantContextValue, @Param("id") id: string, @Body() d: UpdateDiscountDto) { return this.discounts.update(c.tenantId, id, d); } @Delete(":id") @Roles(UserRole.PARTNER_ADMIN, UserRole.ACTIVITY_MANAGER) remove(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.discounts.remove(c.tenantId, id); } }
