import { Body, Controller, Delete, Get, Param, Patch, Post, Query, UseGuards } from "@nestjs/common";
import { ApiHeader, ApiTags } from "@nestjs/swagger";
import { UserRole } from "@prisma/client";
import { AccessTokenGuard } from "../../auth/auth.guard"; import { Roles } from "../../auth/roles.decorator"; import { RolesGuard } from "../../auth/roles.guard"; import { TenantAccessGuard } from "../../auth/tenant.guard"; import { TenantContext, TenantContextValue } from "../../common/tenant-context"; import { CancellationPoliciesService } from "./cancellation-policies.service"; import { CreatePolicyDto, PolicyQueryDto, UpdatePolicyDto } from "./dto/policy.dto";
@ApiTags("cancellation-policies") @ApiHeader({ name: "x-tenant-id", required: true }) @Controller("cancellation-policies") @UseGuards(AccessTokenGuard, TenantAccessGuard, RolesGuard)
export class CancellationPoliciesController { constructor(private readonly policies: CancellationPoliciesService) {} @Get() list(@TenantContext() c: TenantContextValue, @Query() q: PolicyQueryDto) { return this.policies.list(c.tenantId, q); } @Post() @Roles(UserRole.PARTNER_ADMIN, UserRole.ACTIVITY_MANAGER) create(@TenantContext() c: TenantContextValue, @Body() d: CreatePolicyDto) { return this.policies.create(c.tenantId, d); } @Get(":id") get(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.policies.get(c.tenantId, id); } @Patch(":id") @Roles(UserRole.PARTNER_ADMIN, UserRole.ACTIVITY_MANAGER) update(@TenantContext() c: TenantContextValue, @Param("id") id: string, @Body() d: UpdatePolicyDto) { return this.policies.update(c.tenantId, id, d); } @Delete(":id") @Roles(UserRole.PARTNER_ADMIN) remove(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.policies.remove(c.tenantId, id); } }
