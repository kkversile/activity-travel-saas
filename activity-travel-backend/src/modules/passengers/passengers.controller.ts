import { Body, Controller, Delete, Get, Param, Patch, Post, Query, UseGuards } from "@nestjs/common"; import { ApiHeader, ApiTags } from "@nestjs/swagger"; import { UserRole } from "@prisma/client"; import { AccessTokenGuard } from "../../auth/auth.guard"; import { Roles } from "../../auth/roles.decorator"; import { RolesGuard } from "../../auth/roles.guard"; import { TenantAccessGuard } from "../../auth/tenant.guard"; import { TenantContext, TenantContextValue } from "../../common/tenant-context"; import { CreatePassengerDto, PassengerQueryDto, UpdatePassengerDto } from "./dto/passenger.dto"; import { PassengersService } from "./passengers.service";
@ApiTags("passengers") @ApiHeader({ name: "x-tenant-id", required: true }) @Controller("passengers") @UseGuards(AccessTokenGuard, TenantAccessGuard, RolesGuard)
export class PassengersController { constructor(private readonly passengers: PassengersService) {} @Get() list(@TenantContext() c: TenantContextValue, @Query() q: PassengerQueryDto) { return this.passengers.list(c.tenantId, q); } @Post() @Roles(UserRole.PARTNER_ADMIN, UserRole.BOOKING_AGENT) create(@TenantContext() c: TenantContextValue, @Body() d: CreatePassengerDto) { return this.passengers.create(c.tenantId, d); } @Get(":id") get(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.passengers.get(c.tenantId, id); } @Patch(":id") @Roles(UserRole.PARTNER_ADMIN, UserRole.BOOKING_AGENT) update(@TenantContext() c: TenantContextValue, @Param("id") id: string, @Body() d: UpdatePassengerDto) { return this.passengers.update(c.tenantId, id, d); } @Delete(":id") @Roles(UserRole.PARTNER_ADMIN) remove(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.passengers.remove(c.tenantId, id); } }
