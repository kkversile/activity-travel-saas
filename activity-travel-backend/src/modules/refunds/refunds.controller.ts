import { Body, Controller, Get, Param, Post, Query, UseGuards } from "@nestjs/common"; import { ApiHeader, ApiTags } from "@nestjs/swagger"; import { UserRole } from "@prisma/client"; import { AccessTokenGuard } from "../../auth/auth.guard"; import { Roles } from "../../auth/roles.decorator"; import { RolesGuard } from "../../auth/roles.guard"; import { TenantAccessGuard } from "../../auth/tenant.guard"; import { TenantContext, TenantContextValue } from "../../common/tenant-context"; import { CreateRefundDto, RefundQueryDto } from "./dto/refund.dto"; import { RefundsService } from "./refunds.service";
@ApiTags("refunds") @ApiHeader({ name: "x-tenant-id", required: true }) @Controller("refunds") @UseGuards(AccessTokenGuard, TenantAccessGuard, RolesGuard)
export class RefundsController { constructor(private readonly refunds: RefundsService) {} @Get() list(@TenantContext() c: TenantContextValue, @Query() q: RefundQueryDto) { return this.refunds.list(c.tenantId, q); } @Post() @Roles(UserRole.PARTNER_ADMIN, UserRole.BOOKING_AGENT) create(@TenantContext() c: TenantContextValue, @Body() d: CreateRefundDto) { return this.refunds.create(c.tenantId, d); } @Get(":id") get(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.refunds.get(c.tenantId, id); } @Post(":id/approve") @Roles(UserRole.PARTNER_ADMIN) approve(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.refunds.approve(c.tenantId, id); } @Post(":id/process") @Roles(UserRole.PARTNER_ADMIN) process(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.refunds.process(c.tenantId, id); } }
