import { Body, Controller, Get, Param, Patch, UseGuards } from "@nestjs/common"; import { ApiHeader, ApiTags } from "@nestjs/swagger"; import { AccessTokenGuard } from "../../auth/auth.guard"; import { Roles } from "../../auth/roles.decorator"; import { RolesGuard } from "../../auth/roles.guard"; import { TenantAccessGuard } from "../../auth/tenant.guard"; import { UserRole } from "@prisma/client"; import { TenantContext, TenantContextValue } from "../../common/tenant-context"; import { SettingsService } from "./settings.service"; import { UpdateGeneralSettingsDto, UpdateSettingsSectionDto } from "./dto/settings.dto";
@ApiTags("settings") @ApiHeader({ name: "x-tenant-id", required: true }) @Controller("settings") @UseGuards(AccessTokenGuard, TenantAccessGuard, RolesGuard)
export class SettingsController { constructor(private readonly settings: SettingsService) {} @Get("general") get(@TenantContext() c: TenantContextValue) { return this.settings.get(c.tenantId); } @Patch("general") @Roles(UserRole.PARTNER_ADMIN) update(@TenantContext() c: TenantContextValue, @Body() d: UpdateGeneralSettingsDto) { return this.settings.update(c.tenantId, d); } @Get(":section") getSection(@TenantContext() c: TenantContextValue, @Param("section") section: string) { return this.settings.getSection(c.tenantId, section); } @Patch(":section") @Roles(UserRole.PARTNER_ADMIN) updateSection(@TenantContext() c: TenantContextValue, @Param("section") section: string, @Body() d: UpdateSettingsSectionDto) { return this.settings.updateSection(c.tenantId, section, d.value ?? {}); } }
