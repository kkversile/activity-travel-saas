import { Body, Controller, Get, Header, Param, Post, Query, StreamableFile, UseGuards } from "@nestjs/common"; import { ApiHeader, ApiTags } from "@nestjs/swagger"; import { UserRole } from "@prisma/client"; import { AccessTokenGuard } from "../../auth/auth.guard"; import { Roles } from "../../auth/roles.decorator"; import { RolesGuard } from "../../auth/roles.guard"; import { TenantAccessGuard } from "../../auth/tenant.guard"; import { TenantContext, TenantContextValue } from "../../common/tenant-context"; import { CreateInvoiceDto, InvoiceQueryDto } from "./dto/invoice.dto"; import { InvoicesService } from "./invoices.service";
@ApiTags("invoices") @ApiHeader({ name: "x-tenant-id", required: true }) @Controller("invoices") @UseGuards(AccessTokenGuard, TenantAccessGuard, RolesGuard)
export class InvoicesController { constructor(private readonly invoices: InvoicesService) {} @Get() list(@TenantContext() c: TenantContextValue, @Query() q: InvoiceQueryDto) { return this.invoices.list(c.tenantId, q); } @Post() @Roles(UserRole.PARTNER_ADMIN, UserRole.BOOKING_AGENT) create(@TenantContext() c: TenantContextValue, @Body() d: CreateInvoiceDto) { return this.invoices.create(c.tenantId, d); } @Get(":id/pdf") @Header("Content-Type", "application/pdf") pdf(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.invoices.pdf(c.tenantId, id).then((buffer) => new StreamableFile(buffer)); } @Get(":id") get(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.invoices.get(c.tenantId, id); } @Post(":id/issue") @Roles(UserRole.PARTNER_ADMIN) issue(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.invoices.issue(c.tenantId, id); } }
