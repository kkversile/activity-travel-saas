import { Body, Controller, Delete, Get, Param, Patch, Post, Query, UseGuards } from "@nestjs/common"; import { ApiHeader, ApiTags } from "@nestjs/swagger"; import { UserRole } from "@prisma/client"; import { AccessTokenGuard } from "../../auth/auth.guard"; import { Roles } from "../../auth/roles.decorator"; import { RolesGuard } from "../../auth/roles.guard"; import { TenantAccessGuard } from "../../auth/tenant.guard"; import { TenantContext, TenantContextValue } from "../../common/tenant-context"; import { CreateVariantDto, UpdateVariantDto, VariantQueryDto } from "./dto/variant.dto"; import { VariantsService } from "./variants.service";
@ApiTags("variants") @ApiHeader({ name: "x-tenant-id", required: true }) @Controller("variants") @UseGuards(AccessTokenGuard, TenantAccessGuard, RolesGuard)
export class VariantsController { constructor(private readonly variants: VariantsService) {} @Get() list(@TenantContext() c: TenantContextValue, @Query() q: VariantQueryDto) { return this.variants.list(c.tenantId, q); } @Post() @Roles(UserRole.PARTNER_ADMIN, UserRole.ACTIVITY_MANAGER) create(@TenantContext() c: TenantContextValue, @Body() d: CreateVariantDto) { return this.variants.create(c.tenantId, d); } @Get(":id") get(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.variants.get(c.tenantId, id); } @Patch(":id") @Roles(UserRole.PARTNER_ADMIN, UserRole.ACTIVITY_MANAGER) update(@TenantContext() c: TenantContextValue, @Param("id") id: string, @Body() d: UpdateVariantDto) { return this.variants.update(c.tenantId, id, d); } @Delete(":id") @Roles(UserRole.PARTNER_ADMIN, UserRole.ACTIVITY_MANAGER) remove(@TenantContext() c: TenantContextValue, @Param("id") id: string) { return this.variants.remove(c.tenantId, id); } }
